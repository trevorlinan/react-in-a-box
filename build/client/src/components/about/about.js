import React from 'react';
import styles from './about.scss';

export default () => (
	<div className={styles.aboutContainer}>
		<p>Hi! I'm React in a Box, an isomporhic React boilerplate utilizing a Node/Express server, Redux, React Router v4 and Webpack.</p>
	</div>	
);
